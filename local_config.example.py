# Copiez ce fichier vers `local_config.py` (ignoré par git) et remplissez-le.
# `local_config.py` sera chargé automatiquement par server.py au démarrage.
# Les variables d'environnement restent prioritaires si elles sont définies.

OPENROUTER_API_KEY = "sk-or-v1-collez-votre-cle-ici"

# Optionnel — modèle par défaut. Laissez commenté pour utiliser openai/gpt-3.5-turbo.
# OPENROUTER_MODEL = "google/gemini-2.0-flash-exp:free"
